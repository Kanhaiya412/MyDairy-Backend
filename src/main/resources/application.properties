# --- DATABASE CONFIGURATION ---
# New AWS RDS Endpoint (US East N. Virginia)
spring.datasource.url=jdbc:mysql://my-dairy-db.c4loe8oky2e3.us-east-1.rds.amazonaws.com:3306/MYFARMER_DB?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true

# Credentials (Make sure these match what you just set!)
spring.datasource.username=admin
# ?? REPLACE THIS with the new password you typed in the wizard
spring.datasource.password=KartikDairy

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- HIBERNATE CONFIGURATION ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- SERVER CONFIGURATION ---
server.address=0.0.0.0
server.port=8080

# --- KAFKA CONFIGURATION (Localhost) ---
# Ensure you have Kafka running on your laptop at port 9092
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.acks=1
spring.kafka.producer.properties.max.request.size=1048576
spring.kafka.producer.properties.linger.ms=5

spring.kafka.consumer.group-id=mydairy-consumer
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer